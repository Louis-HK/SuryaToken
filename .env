# Ethereum node endpoint (could be a local node or a provider like Infura/Alchemy/your private RPC)
RPC_URL=https://your-eth-node

# Private key used for test/deployment — USE A DEVELOPMENT KEY, never your main wallet
PRIVATE_KEY=0xYOUR_DEV_PRIVATE_KEY_HERE

# Chainlink (or other oracle) API key if you consume a hosted oracle service
CHAINLINK_API_KEY=your_chainlink_api_key

# URL or endpoint where energy generation data is fetched/validated
ENERGY_DATA_FEED_URL=https://your.energy.data.source/api

# Optional: environment to distinguish (e.g., testnet / staging / prod)
ENVIRONMENT=development
